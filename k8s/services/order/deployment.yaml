apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: ecommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: gcr.io/ecommerce-distributed-system/order-service:3.0
          imagePullPolicy: Always
          ports:
            - containerPort: 8093

          envFrom:
            - configMapRef:
                name: order-config
            - secretRef:
                name: order-secret